- project = nil unless defined?(:project)
- unless project
  - if params[:project_id]
    - project = project.find(grab_param(:project_list, :project_id)) rescue nil
%table.tabular{:cellspacing => 0}
  - if project
    %thead
      %tr
        %th
          %span Lists
          .actions= link_to image_tag('/images/fluxx_engine/theme/default/icons/add.png', :class=> 'add-button'), new_project_list_path(:project_list => {:project_id => project.id}), :class => 'to-modal', 'data-is-success' => '.show', 'data-on-success' => 'refreshCaller,close', :title => "Add Project List"
  %tbody
    - if !models || models.empty?
      %tr
        %td= "No Projects"
    - else
      - models.each do |model|
        %tr
          %td
            = model.title
            - if current_user.has_delete_for_model? model
              %span.delete-button= link_to image_tag("/images/fluxx_engine/theme/default/icons/delete.png"), project_list_path(model), {:class => 'as-delete', 'data-on-success' => 'refreshCaller'}