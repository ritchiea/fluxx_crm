%h2= model.name

%h3
  Subject
%p= model.subject

%h3
  Body
%p= model.body

%h3
  Recipients
%p= model.recipients.map(&:full_name).to_sentence

= render :partial => "show_#{model.class.name.underscore}", :locals => { :model => model }
